This document already has content and was rejected by a reviewer. 

<reviewer_feedback>
${review}
</reviewer_feedback>

<current_content>
${content}
</current_content>

<source_nodes> These are the node ids the previous version was grounded in - a convenient, known-valid starting 
point, not a boundary. Re-verify the ones the feedback disputes directly with get_node_synthesis or get_node_content 
to save a step, but explore freely with get_root_nodes, get_children, or get_parents for anything the revision 
genuinely needs beyond them.

${source_node_ids} </source_nodes>


Address the feedback precisely, per your instructions on revising.